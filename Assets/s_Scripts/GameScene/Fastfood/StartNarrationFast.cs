using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StartNarrationFast : MonoBehaviour
{
    public AudioSource a;

    // Start is called before the first frame update
    void Start()
    {
        a.Play();
    }
}
